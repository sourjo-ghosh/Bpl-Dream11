import React from 'react';

const SelectedPlayers = ({SelectedPlayersTab, setSelectedPlayersTab}) => {
    console.log(SelectedPlayersTab.length)
    return (
        <div>
            hello world 
        </div>
    );
};

export default SelectedPlayers;